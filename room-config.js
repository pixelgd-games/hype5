// room-config.js

const ROOM_CONFIG = {
  PORT: 2567,
  HOST: "localhost",
  ROOM_CLEANUP_DELAY_MS: 10000,
  MOVE_RATE_LIMIT_MS: 80,
  MAX_CLIENTS_PER_ROOM: 10
};

module.exports = {
  ROOM_CONFIG
};